#include <bits/stdc++.h>
using namespace std;
#define ll long long

void solve()
{
    string a, b;
    cin >> a >> b;
    int n1 = a.size(), n2 = b.size(), n = a.size() + 1; // n1 > n2
    int x[n1], y[n2], z[n];
    for (int i = 0; i < n1; i++)
        x[i] = a[i] - '0';
    for (int i = 0; i < n1; i++)
        x[i] = a[i] - '0';
}

int main()
{
    int t;
    cin >> t;
    while (t--) {
    }
}