#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define endl '\n'

int main()
{
    ifstream in;
    ofstream out;
    in.open("PTIT.in");
    out.open("PTIT.out");
    string s;
    while(getline(in, s))
        out << s << endl;
    in.close();
    out.close();
}
